import type { ToastType } from "../types/util/toast.type"

const TOAST_KEYS : {
    INFO : ToastType,
    WARNING : ToastType,
    SUCCESS : ToastType,
    ERROR : ToastType
} = {
    INFO : "info",
    WARNING : "warning",
    SUCCESS : "success",
    ERROR : "error"
}

export default TOAST_KEYS